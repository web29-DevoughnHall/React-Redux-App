const initialState = {
    stuff: [],
    isLoading: false,
    error: ''
}

export const shibeReducer = (state = initialState, action) => {
    switch (action.type) {
        default:

            return state;
    }
}